# CLAUDE.md — 标注平台（Label Studio 二开）

## 项目身份
- Label Studio 开源二开（HumanSignal，Apache 2.0——保留 LICENSE/NOTICE，修改需声明）
- 技术栈：Django + DRF 后端 / React-TS 前端（web/）/ SQLite→PostgreSQL / Docker
- 项目强度：**S2 学习/内部项目**——守铁律+流程骨架；任何产出要进交付，先按 S3 补流程
- 总规范：`/Users/pp/working/work/知识库/AI辅助开发/03_编制规范/AI辅助开发工作规范.md`；本文件是其项目实例，冲突时以总规范及上级 CLAUDE.md 为准

## 开局与收工（每会话必做）
- 开局：读 `claude/_进度与待办.md`，一句话报状态："当前在 X 阶段，本次做 Y"，确认后再动手
- 收工：回写 `claude/_进度与待办.md`（做了什么、commit 摘要、下一步）；没做完/做错的照实写
- 阶段过关标准查总规范第三章对应节

## 开发铁律（违反即停）
1. **Git 三底线**：远端仓库建好前不改任何代码；每任务一个 commit，写清改了什么为什么；main/dev 不直接实验
2. **小步**：一次只做一个需求；每次改动后系统必须可运行；出错先问"回滚到上个 commit 重来是否更快"
3. **不懂不合入**：pp 看不懂的关键改动，用业务语言讲解；仍存疑送 Codex 审，再存疑不合入
4. **外挂优先**：每个需求先回答"能不能不改核心源码"，顺序=标注模板配置 > API/SDK 外挂 > ML Backend > 改核心；改核心需 pp 明确批准并记入 `claude/00_开发决策记录.md`
5. **方案先行**：动手前先出方案（动哪些文件/影响面/风险/验证方案），pp 确认后才实现

## 安全红线
- 密钥/token/密码不进代码不进 Git；一律 `.env`（确认已在 .gitignore）；提交挂 Gitleaks 拦截
- 练手一律假数据/脱敏样本，禁用公司真实业务数据
- 对接内部系统只读账号起步；删除/更新类数据库语句先展示、pp 放行后执行
- 大改上游文件前提示升级冲突代价（总规范 A7）

## 约定
- 中文沟通，技术术语保留英文；结论先行，宁短勿长
- 任务用任务卡五栏：任务/背景/目标/边界/验证
- 主张"项目里有 X"必须给文件路径；说"已测试"必须附测试输出
- 临时脚本放 `claude/tmp/`，用完即删；不动 pp 的既有文件除非确认
